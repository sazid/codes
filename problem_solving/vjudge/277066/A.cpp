#include <bits/stdc++.h>
using namespace std;

// int a[int(1e5) + 5];
bitset<int(2e5)+5> flags;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	memset(flag, -1, sizeof(flag));
	
	int n, q;
	cin >> n;
	
	vector<int> a(n);
	
	for (int &x: a) cin >> x;
	
	
	
	return 0;
}
