#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef unsigned long long ull;

int arr[(int)1e6 + 5];

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int n;
	cin >> n;
	
	arr[0] = 0;
	for (int i = 1; i <= n; ++i) {
		cin >> arr[i];
	}
	
	for (int i = 1; i <= n-1; ++i) {
		
	}
	
	return 0;
}

