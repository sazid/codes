// https://open.kattis.com/problems/carrots
#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n, f;
	string s;
	cin >> n >> f;
	while (n--) cin >> s;
	cout << f << endl;
	return 0;
}

